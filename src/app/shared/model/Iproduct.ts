export interface Iproduct{
    id: string;
    name: string;
    pstatus: string;
}

export type Iprod={
    AVAILABLE: "Available",
    OUT_OF_STOCK: "Out of Stock",
    DISCONTINUED: "Discontinued"    
}